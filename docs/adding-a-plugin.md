# Adding a plugin

Pin the complete source first. Register manifests, hooks, commands, agents, MCP definitions, dependencies, and lifecycle behavior. Generate only the narrow host registration needed. Detect duplicate skill exposure between plugin and portable links before activation.
