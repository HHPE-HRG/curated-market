#!/usr/bin/env node
import fs from 'node:fs';import path from 'node:path';import {fileURLToPath} from 'node:url';const root=path.resolve(path.dirname(fileURLToPath(import.meta.url)),'..');const source=path.join(root,'registry/overlays/wrappers');const target=path.join(root,'registry/adapters/codex/marketplace/plugins/hhpe-registry/skills');for(const name of ['ast-grep','registry-health','stack-router']){const from=path.join(source,name),to=path.join(target,name);fs.rmSync(to,{recursive:true,force:true});fs.cpSync(from,to,{recursive:true,dereference:true});}console.log('generated Codex HHPE adapter skills from canonical overlays');
