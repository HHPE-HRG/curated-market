# Adding a package

Clone the complete repository into `registry/packages/<id>/<commit>`, verify license and clean state, record commit and tree, inventory every component, add a package ADR, regenerate manifests, run security review and parity fixtures, then expose only proven entry points.
