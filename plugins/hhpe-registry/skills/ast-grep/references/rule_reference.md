# Rule reference

Prefer repository-local `sgconfig.yml` and named YAML rules for repeated searches. Constrain `language`, `rule`, `inside`, `has`, and `not` clauses independently; validate each added constraint against positive and negative fixtures. Apply fixes only after search results are stable and an exact diff has been reviewed.
