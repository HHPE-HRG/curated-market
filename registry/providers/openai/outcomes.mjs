/** Re-export shared outcome vocabulary (M1 import path preserved). */
export {
  OutcomeClass,
  DURABLE_UNAVAILABLE_OUTCOMES,
  TRANSIENT_OUTCOMES,
  isDurableUnavailable,
  isTransientDispatchFailure,
} from '../outcomes.mjs';
